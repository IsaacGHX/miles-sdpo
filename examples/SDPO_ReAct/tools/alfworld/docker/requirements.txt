# ALFWorld's own pins (gymnasium/stable-baselines3 versions the alfworld
# package is validated against, per its README) -- these deliberately do NOT
# need to match the training image's torch/transformers stack, which is
# exactly why this sidecar is its own container (see Dockerfile's docstring).
gymnasium==0.29.1
stable-baselines3==2.6.0
alfworld
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
pyyaml==6.*
httpx==0.27.*
